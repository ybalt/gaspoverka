create table "PUBLIC".DEV
(
	Type VARCHAR(100) NOT NULL PRIMARY KEY,
	Channel INTEGER,
	UD INTEGER,
	PL INTEGER,
	IC INTEGER,
	MIC INTEGER
)

create table "PUBLIC".DEV_MR
(
    TYPE VARCHAR(100) NOT NULL PRIMARY KEY,
    VL DOUBLE,
    VH DOUBLE,
    Error DOUBLE

)

create table "PUBLIC".CALIBRATION
(
    CHANNEL INTEGER NOT NULL PRIMARY KEY,
    POINT INTEGER,
    X DOUBLE,
    Y DOUBLE,
    YS DOUBLE
)